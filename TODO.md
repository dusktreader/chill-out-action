# TODO

- Once `chill-out` supports `--format json` (dusktreader/chill-out#10), populate
  the fix PR body automatically with the list of pinned packages and their versions
  instead of the current generic summary.
